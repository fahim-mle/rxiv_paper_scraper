# Core dependencies for arXiv Paper Scraper
aiohttp>=3.8.0
aiofiles>=22.0.0
asyncio
logging

# Database
pymongo>=4.0.0

# Web scraping and parsing
beautifulsoup4>=4.11.0
lxml>=4.9.0
requests>=2.28.0

# PDF processing (fallback if Unstructured MCP not available)
PyPDF2>=3.0.0
pdfplumber>=0.7.0

# Data processing
numpy>=1.21.0
pandas>=1.5.0

# HTTP client utilities
httpx>=0.24.0

# Configuration and utilities
pydantic>=1.10.0
python-dotenv>=0.20.0
pyyaml>=6.0.0

# Scheduling and process management
schedule>=1.2.0
psutil>=5.9.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Optional dependencies for enhanced functionality
# These will be installed only if MCP servers are not available

# NLP preprocessing (fallback for Unstructured MCP)
nltk>=3.8
spacy>=3.4.0

# Mathematical expression parsing (fallback for arXiv MCP)
sympy>=1.11.0